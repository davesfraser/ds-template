_templates_suffix: .jinja
_subdirectory: template

project_name:
  type: str
  help: What is the title of your project? (Human readable)
  default: Data science project

project_slug:
  type: str
  help: What is the repository and package distribution name? (kebab-case, e.g. my-ds-project)
  default: data-science-project
  validator: >-
    {% if not (project_slug | regex_search('^[a-z][a-z0-9-]+$')) %}
    project_slug must be lowercase kebab-case — letters, numbers, and hyphens only (e.g. my-ds-project)
    {% endif %}

package_name:
  type: str
  help: What is the Python import package name? (snake_case, e.g. my_ds_project)
  default: data_science_project
  validator: >-
    {% if not (package_name | regex_search('^[a-z][a-z0-9_]+$')) %}
    package_name must be snake_case — letters, numbers, and underscores only (e.g. my_ds_project)
    {% endif %}

project_description:
  type: str
  help: Provide a a one line description for pyproject and README
  default: Modern Python data science project

author_name:
  type: str
  help: What is the name of the main author or team name?
  default: Your Name

python_min_version:
  type: str
  help: What is the minimum supported Python version (recommend 3.14)
  default: "3.14"

_exclude:
  - "uv.lock"
  - ".venv"
  - ".pytest_cache"
  - ".ruff_cache"
  - "__pycache__"
  - ".github/workflows/template-smoke.yaml"
