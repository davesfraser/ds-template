# {{ project_name }}

[![uv](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/uv/main/assets/badge/v0.json)](https://github.com/astral-sh/uv)
[![Ruff](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json)](https://github.com/astral-sh/ruff)
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

{{ project_description }}

## First Steps

```bash
uv sync
uv run pre-commit install
git add .
git add uv.lock
git commit -m "Initial project setup"
```

## Common Commands

Format code:

```bash
uv run ruff format .
```

Lint:

```bash
uv run ruff check src tests
```

Type check:

```bash
uv run mypy src tests
```

Run tests:

```bash
uv run pytest
```

Build the package:

```bash
uv build
```

## Project Layout

```text
src/{{ package_name }}/     reusable package code
tests/                      pytest tests
tests/fixtures/             shared test fixtures
docs/                       project documentation
docs/decisions/             design decision records
examples/                   small runnable examples
scripts/                    developer and maintenance scripts
.github/skills/             project-specific AI coding agent skills
.github/workflows/          GitHub Actions CI
AGENTS.md                   AI assistant coding instructions
pyproject.toml              project metadata and tool configuration
uv.lock                     locked dependencies
```

## Extension Points

This project includes a few empty or documentation-only folders that downstream
teams can build on without changing the base layout:

- Put project documentation in `docs/`.
- Record design decisions in `docs/decisions/`.
- Add small runnable examples in `examples/`.
- Add checked-in developer scripts in `scripts/`.
- Add reusable test fixtures in `tests/fixtures/`.
- Add real AI coding agent skills in `.github/skills/`.

Specialised templates may add extra dependency groups, commands, folders, and
AI assistant rules for their domain. Keep reusable project code in `src/` and
document any domain-specific workflow here.

<!-- project-template:extension:readme-domain:start -->
<!-- Add specialised template guidance here. -->
<!-- project-template:extension:readme-domain:end -->

## Dependencies

Use `uv add` to keep project metadata and the lockfile in sync.

Examples:

```bash
uv add requests
uv add --dev pytest-mock
```

If code in `src/` imports a package at runtime, that package belongs in base
dependencies. If a package is only used for development, add it to the dev group.

## Lockfile

This project expects `uv.lock` to be committed.

The first `uv sync` creates it. CI installs from the committed lockfile with
`uv sync --frozen`.

Dependabot is configured to open weekly PRs for uv dependency and GitHub Actions
updates. CI validates each Dependabot PR automatically.
