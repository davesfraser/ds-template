# {{ project_name }}

{{ project_description }}

This repository contains a modern Python project.
AI coding assistants must follow the rules in this document when generating code.

---

# Core rules

RULE: use uv
RULE: keep reusable code in src/
RULE: write tests
RULE: run ruff, mypy and pytest
RULE: do not hardcode secrets
RULE: prefer typed, small, testable functions
RULE: keep IO separate from pure logic where practical

---

# Project stack

- Python {{ python_min_version }}+
- uv for package and environment management
- ruff for linting and formatting
- mypy for type checking
- pytest for tests

---

# Project structure

```text
src/{{ package_name }}/     reusable project code
tests/                      pytest tests
tests/fixtures/             shared test fixtures
docs/                       project documentation
docs/decisions/             design decision records
examples/                   small runnable examples
scripts/                    developer and maintenance scripts
.github/skills/             project-specific AI coding agent skills
```

---

# Commands

All commands must be executed using `uv run`:

```bash
uv run pytest              # run tests
uv run mypy src tests      # type check
uv run ruff check .        # lint
uv run ruff format .       # format
```

---

# Linting and type checking

This project uses ruff for linting and formatting, and mypy for type checking.
Generated code must pass both without errors.

Key ruff settings:

- Line length: 100
- Enabled rule sets: E4/E7/E9, F, I, UP, B, SIM, C4, PIE, RUF, ANN
- `src/` functions must have type annotations on all arguments and return values
- `tests/` are exempt from annotation rules if configured that way in pyproject

Key mypy settings:

- `check_untyped_defs = true` — untyped functions are still checked internally

RULE: All code in `src/` must pass `ruff check` and `mypy` without errors.
RULE: Do not add `# type: ignore` or `# noqa` comments unless there is a genuine false positive — fix the code instead.

---

# Extension points

RULE: Put downstream-specific AI coding agent skills in `.github/skills/`.
RULE: Each specialised skill must live at `.github/skills/<skill-name>/SKILL.md`.
RULE: Put project documentation in `docs/` and design decisions in `docs/decisions/`.
RULE: Put checked-in developer scripts in `scripts/`.
RULE: Put small runnable examples in `examples/`.
RULE: Put shared test fixtures in `tests/fixtures/`.

---

# Downstream rules

Downstream templates should append specialised rules in this section.
Keep the generic base rules above unless a downstream project has a clear
reason to override them.

<!-- project-template:extension:downstream-rules:start -->
<!-- Add project-type-specific rules here. -->
<!-- project-template:extension:downstream-rules:end -->
